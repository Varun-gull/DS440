# Frontend

Place the user-facing web application here, including pages, reusable components, styles, client-side state, and frontend-specific tests.

Recommended internal structure after the framework is selected:

```text
frontend/
├── app/ or src/
├── components/
├── public/
├── tests/
└── package.json
```
