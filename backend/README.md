# Backend

Place server APIs, authentication logic, business rules, external job-source integrations, and server-side configuration here.

Keep provider credentials in ignored environment files or deployment secrets, never in source code.
